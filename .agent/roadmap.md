# Roadmap

## Now

- Fix or isolate the UI test runner launch failure so full scheme validation can run cleanly.
- Commit the scoped change set only after full validation is clean.

## Next

- Manually verify Dynamic Type and VoiceOver result-row behavior once simulator accessibility is healthy.
- Audit current screenshot set against latest UI.
- Add deeper formula edge-case coverage by class, weight, and drivetrain.

## Later

- Split large Swift files only when active work naturally touches them.
- Refresh community/Reddit research before any public launch posts.
- Continue formula accuracy tests by discipline and drivetrain.
